# File meant to be sourced in the ~/.bashrc file, i.e. like this :
# . ~/.emacs.d/.bashrc
# Contains everything I want to add in the bashrc file. Put here to
# track it with git, and as I will surely always install emacs on
# every setup

# Setup the default ledger journal file
export LEDGER_FILE=$HOME/Documents/org/ledger.dat
